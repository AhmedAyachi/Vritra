import {ViewProps,ViewInterface as View} from "../index";


/**
 * Same as View component but with the default global cherry styling
 * @param props 
 * Every Cherry component root should use this component
 */
export default function CherryView(props:ViewProps):View;

